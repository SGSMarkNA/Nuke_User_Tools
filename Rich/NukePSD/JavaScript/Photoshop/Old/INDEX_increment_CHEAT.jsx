// Four zeroes padding...var ZEROS = "0000";var FIRST_FRAME = "/Users/richbobo/Dropbox/richbobo/NUKE/Scripts/Save_PSD_From_Nuke_TESTING/img/PNG/Background/Background_0001.png"INDEX = (FIRST_FRAME.split(".")[0]);INDEX = (INDEX.split("_"));INDEX = (INDEX [INDEX.length -1]);// Convert to number so we can increment it...INDEX = Number(INDEX);//alert(typeof(INDEX));// Increment...INDEX++//alert(INDEX);// Convert back to string, so we can use hack to add padding, since there doesn't seem to be a method that works...INDEX = INDEX.toString();alert(INDEX);// Add needed number of leading zeroes...INDEX = ZEROS.substr(0, ZEROS.length - INDEX.length) + INDEX;alert(INDEX);///////////////////////////////////////////////////////   REF: https://forums.adobe.com/thread/286105/////////////////////////////////////////////////////// Four zeroes//var s1 = "0000";// Get field value//var s2 = getField("AccountNumber").valueAsString;// Add needed number of leading zeroes//var AccNum = s1.substr(0, s1.length - s2.length) + s2;